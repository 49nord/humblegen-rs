struct Customer {
    name: str,
    id: i32,
    net_worth: f64,
    favorite_color: Color,
    aliases: list[str],
    coords: (i32, i32),
}

enum Color {
    Red,
    Blue,
    Green,
    Rgb(u8, u8, u8),
    Hsv {
        h: u8,
        s: u8,
        v: u8,
    },
}
